import styled from 'styled-components';

const SaleTag = styled.span`
  color: #fff;
  padding-left: 10px;
  background: #e41919;
  font-weight: bold;
  display: flex;
  align-items: center;
  padding-right: 10px;
`;

export default SaleTag;
